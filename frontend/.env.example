# CloudMentor frontend API target examples.
# The browser only needs the backend URL. Keep AI API keys in backend/env.json.

# Local laptop mode with SAM Local backend.
VITE_API_BASE_URL=http://localhost:3000

# For production, GitHub Actions injects the API Gateway URL during the build.
# Do not place AI keys or production credentials in a VITE_* variable.
